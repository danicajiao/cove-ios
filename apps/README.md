# Apps

Client applications. Each subdirectory owns its own build tooling and CI workflow.

| App | Stack | Status |
|---|---|---|
| `ios/` | Swift / SwiftUI, Firebase | Active |
| `web/` | TBD | Planned |

Backend services live in `../services/` (e.g. `services/cove-api/`). Cross-app
concerns (shared types, design tokens) live in `../packages/`.
